# @xenia/ui

Shared React component library + design tokens, consumed by `apps/web`,
`apps/guest-concierge`, and `apps/cleaner-pwa` so the three surfaces share one
visual language. Seeded with `StatusBadge`; grow it with shadcn/ui primitives as
the dashboard is built.
