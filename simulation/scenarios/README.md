# simulation/scenarios

Named, seeded scenarios that compose the channel/guest/cleaner/lock simulators
into a single repeatable run — e.g. `busy-weekend` (3 same-day turnovers, 1
cancellation, 1 conflicting double-booking attempt). This is the backbone of the
2-minute demo and a deterministic substrate for load tests and AI evals.
