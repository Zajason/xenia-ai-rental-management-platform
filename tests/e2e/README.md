# tests/e2e

Playwright browser tests over the web dashboard and the guest concierge — the
critical user journeys (log in, view arrivals, run the cleaner flow, ask the
concierge a question and get a grounded answer).
